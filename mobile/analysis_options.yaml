# Static analysis — PocketPillar Flutter
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    - always_declare_return_types
    - avoid_print
    - prefer_single_quotes
    - unawaited_futures
    - avoid_catches_without_on_clauses
    - prefer_final_locals
